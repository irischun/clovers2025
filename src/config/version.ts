// Application version configuration
// Update this file when releasing new versions

export const APP_VERSION = "67.1.0";
// Version 67.1.0: Auth recovery now routes password-reset callbacks back to
// /auth correctly and shows a clear fallback when a reset link is expired or invalid,
// so users are never left on a blank page during password recovery.
export const VERSION_NAME = "Version";
export const VERSION_DATE = "2026-05-23";
